apiVersion: v1
kind: Service
metadata:
  name: transaction
  namespace: pointofsale
spec:
  selector:
    app: transaction
  ports:
    - name: grpc
      port: 50058
      targetPort: 50058
  type: ClusterIP
