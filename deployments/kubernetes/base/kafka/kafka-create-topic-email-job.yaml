apiVersion: batch/v1
kind: Job
metadata:
  name: kafka-create-topic-email
  namespace: point-of-sale
  labels:
    app: kafka-create-topic-email
spec:
  backoffLimit: 4
  template:
    spec:
      restartPolicy: OnFailure
      containers:
        - name: create-topics
          image: apache/kafka:3.7.0
          command: ["/bin/sh", "-c"]
          args:
            - |
              set -e
              echo "[KAFKA] Creating email-service topics (idempotent --if-not-exists)"
              for topic in \
                email-service-topic-auth-register \
                email-service-topic-auth-forgot-password \
                email-service-topic-auth-verify-code-success \
                email-service-topic-merchant-create \
                email-service-topic-merchant-update-status \
                email-service-topic-merchant-document-create \
                email-service-topic-merchant-document-update-status \
                email-service-topic-transaction-create \
                email-service-topic-email-retry \
                email-service-topic-email-dlq; do
                kafka-topics.sh --bootstrap-server kafka.point-of-sale.svc.cluster.local:9092 \
                  --create --if-not-exists \
                  --topic "$topic" --partitions 3 --replication-factor 1
              done
              echo "[KAFKA] Email topics ready"
